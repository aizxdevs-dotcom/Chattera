#!/usr/bin/env python3
"""
DEPRECATED: SendGrid test script removed.

The original `scripts/test_sendgrid.py` has been cleared to avoid storing SendGrid API
usage or keys in the repository. If you need a tester, recreate it locally using
your own environment variables (do NOT commit secrets).
"""

import sys


def main():
    print("This script has been removed for security reasons. Create a local test script if needed.")
    return 0


if __name__ == "__main__":
    sys.exit(main())
